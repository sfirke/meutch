[tool.ruff]
# ruff 0.6.9 predates py314; bump alongside ruff.
target-version = "py313"
line-length = 100
extend-exclude = ["migrations"]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

[tool.pylint.main]
jobs = 0
ignore-paths = ["^migrations/"]
py-version = "3.14"

[tool.pylint.messages_control]
disable = [
    "c-extension-no-member",
    "no-member",
]
